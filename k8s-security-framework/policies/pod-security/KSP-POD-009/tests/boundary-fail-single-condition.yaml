apiVersion: v1
kind: Pod
metadata:
  name: pod-009-boundary-ephemeral-additional-boundary-fail
  namespace: policy-test
spec:
  containers:
  - name: secure
    image: harbor.example.com/demo-api:1.0.0
    securityContext:
      capabilities:
        add:
        - NET_BIND_SERVICE
  ephemeralContainers:
  - name: debugger
    image: harbor.example.com/demo-api:1.0.0
    command:
    - sh
    securityContext:
      capabilities:
        add:
        - SYS_ADMIN
