apiVersion: v1
kind: Pod
metadata:
  name: ksp-pod-013-positive-two-compliant
  namespace: policy-test
spec:
  containers:
    - name: secure
      image: harbor.example.com/demo-api:1.0.0
      securityContext:
        runAsUser: 1000
    - name: sidecar
      image: harbor.example.com/demo-api:1.0.0
      securityContext:
        runAsUser: 1000
