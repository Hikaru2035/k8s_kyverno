apiVersion: v1
kind: Pod
metadata:
  name: ksp-pod-013-positive-all-types
  namespace: policy-test
spec:
  containers:
    - name: secure
      image: harbor.example.com/demo-api:1.0.0
      securityContext:
        runAsUser: 1000
  initContainers:
    - name: init
      image: harbor.example.com/demo-api:1.0.0
      securityContext:
        runAsUser: 1000
  ephemeralContainers:
    - name: debugger
      image: harbor.example.com/demo-api:1.0.0
      securityContext:
        runAsUser: 1000
      command: ["sh"]
