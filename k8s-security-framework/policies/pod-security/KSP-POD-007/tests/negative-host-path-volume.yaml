apiVersion: v1
kind: Pod
metadata:
  name: pod-007-negative-hostpath-restricted
  namespace: policy-test
spec:
  volumes:
    - name: host-root
      hostPath:
        path: /
  containers:
    - name: demo-api
      image: harbor.example.com/demo-api:1.0.0
      volumeMounts:
        - name: host-root
          mountPath: /host
