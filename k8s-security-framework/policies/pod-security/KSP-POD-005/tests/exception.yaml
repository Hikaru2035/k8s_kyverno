apiVersion: policies.kyverno.io/v1
kind: PolicyException
metadata:
  name: disallow-host-pid-exception
  namespace: policy-test
spec:
  policyRefs:
  - name: disallow-host-pid
    kind: ValidatingPolicy
  matchConditions:
  - name: match-exempt-resource
    expression: object.metadata.name == 'ksp-pod-005-exception' && object.metadata.namespace
      == 'policy-test'

