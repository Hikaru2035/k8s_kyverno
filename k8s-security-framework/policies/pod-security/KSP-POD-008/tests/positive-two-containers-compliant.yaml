apiVersion: v1
kind: Pod
metadata:
  name: pod-008-positive-two-compliant
  namespace: policy-test
spec:
  containers:
    - name: secure
      image: harbor.example.com/demo-api:1.0.0
      securityContext:
        capabilities:
          drop: [ALL]
    - name: sidecar
      image: harbor.example.com/demo-api:1.0.0
      securityContext:
        capabilities:
          drop: [ALL]
