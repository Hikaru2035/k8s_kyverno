apiVersion: v1
kind: Pod
metadata:
  name: pod-008-negative-first
  namespace: policy-test
spec:
  containers:
    - name: insecure
      image: harbor.example.com/demo-api:1.0.0
      securityContext:
        capabilities:
          drop: [NET_RAW]
    - name: secure
      image: harbor.example.com/demo-api:1.0.0
      securityContext:
        capabilities:
          drop: [ALL]
