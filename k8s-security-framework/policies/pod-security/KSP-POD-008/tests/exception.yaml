apiVersion: kyverno.io/v2
kind: PolicyException
metadata:
  name: drop-all-capabilities-exception
  namespace: policy-test
spec:
  exceptions:
    - policyName: drop-all-capabilities
      ruleNames:
        - drop-all-capabilities
  match:
    any:
      - resources:
          kinds:
            - Pod
          names:
            - ksp-pod-008-exception
          namespaces:
            - policy-test
