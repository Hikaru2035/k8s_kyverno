apiVersion: policies.kyverno.io/v1
kind: PolicyException
metadata:
  name: restrict-host-ports-exception
  namespace: policy-test
spec:
  policyRefs:
  - name: restrict-host-ports
    kind: ValidatingPolicy
  matchConditions:
  - name: match-exempt-resource
    expression: object.metadata.name == 'ksp-pod-014-exception' && object.metadata.namespace
      == 'policy-test'

