apiVersion: policies.kyverno.io/v1
kind: PolicyException
metadata:
  name: require-owner-label-exception
  namespace: policy-test
spec:
  policyRefs:
  - name: require-owner-label
    kind: ValidatingPolicy
  matchConditions:
  - name: match-exempt-resource
    expression: object.metadata.name == 'ksp-meta-002-exception' && object.metadata.namespace
      == 'policy-test'

