apiVersion: kyverno.io/v2
kind: PolicyException
metadata:
  name: require-owner-label-exception
  namespace: policy-test
spec:
  exceptions:
    - policyName: require-owner-label
      ruleNames:
        - require-owner-label
  match:
    any:
      - resources:
          kinds:
            - Deployment
          names:
            - ksp-meta-002-exception
          namespaces:
            - policy-test
