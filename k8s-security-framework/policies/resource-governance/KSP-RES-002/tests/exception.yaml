apiVersion: policies.kyverno.io/v1
kind: PolicyException
metadata:
  name: require-memory-request-exception
  namespace: policy-test
spec:
  policyRefs:
  - name: require-memory-request
    kind: ValidatingPolicy
  matchConditions:
  - name: match-exempt-resource
    expression: object.metadata.name == 'ksp-res-002-exception' && object.metadata.namespace
      == 'policy-test'

