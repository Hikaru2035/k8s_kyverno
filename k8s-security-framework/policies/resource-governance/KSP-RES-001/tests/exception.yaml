apiVersion: policies.kyverno.io/v1
kind: PolicyException
metadata:
  name: require-cpu-request-exception
  namespace: policy-test
spec:
  policyRefs:
  - name: require-cpu-request
    kind: ValidatingPolicy
  matchConditions:
  - name: match-exempt-resource
    expression: object.metadata.name == 'ksp-res-001-exception' && object.metadata.namespace
      == 'policy-test'

