apiVersion: kyverno.io/v2
kind: PolicyException
metadata:
  name: require-cpu-limit-exception
  namespace: policy-test
spec:
  exceptions:
    - policyName: require-cpu-limit
      ruleNames:
        - require-cpu-limit
  match:
    any:
      - resources:
          kinds:
            - Pod
          names:
            - ksp-res-003-exception
          namespaces:
            - policy-test
