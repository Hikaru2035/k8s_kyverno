apiVersion: policies.kyverno.io/v1
kind: PolicyException
metadata:
  name: require-cpu-limit-exception
  namespace: policy-test
spec:
  policyRefs:
  - name: require-cpu-limit
    kind: ValidatingPolicy
  matchConditions:
  - name: match-exempt-resource
    expression: object.metadata.name in ['ksp-res-003-exception', 'ksp-res-003-exception-secondary',
      'ksp-res-003-exception-tertiary'] && object.metadata.namespace == 'policy-test'
