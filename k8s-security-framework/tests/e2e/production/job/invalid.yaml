apiVersion: batch/v1
kind: Job
metadata:
  name: e2e-prod-job-invalid
  namespace: ksp-production
spec:
  template:
    metadata:
      labels:
        app: e2e-prod-job-invalid
    spec:
      restartPolicy: Never
      containers:
        - name: app
          image: nginx:latest
          command: ["sh", "-c", "echo invalid"]
