apiVersion: apps/v1
kind: Deployment
metadata:
  name: e2e-stg-deployment-invalid
  namespace: ksp-staging
spec:
  replicas: 1
  selector:
    matchLabels:
      app: e2e-stg-deployment-invalid
  template:
    metadata:
      labels:
        app: e2e-stg-deployment-invalid
    spec:
      containers:
        - name: app
          image: nginx:latest
          command: ["sh", "-c", "sleep 3600"]
