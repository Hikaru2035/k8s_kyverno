apiVersion: v1
kind: Service
metadata:
  name: demo-app
  namespace: ksp-demo
  labels:
    app.kubernetes.io/name: demo-app
spec:
  selector:
    app.kubernetes.io/name: demo-app
  ports:
  - name: http
    port: 8080
    targetPort: http
